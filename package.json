{
  "name": "house-ui",
  "version": "0.0.0",
  "description": "UI for controlling my house-api",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node  --harmony server.js",
    "dev": "nodemon --harmony server.js",
    "build": "browserify ./public/js/app.js -o ./public/js/bundle.js"
  },
  "author": "Simon McManus",
  "license": "BSD-2-Clause",
  "dependencies": {
    "ninja-blocks": "~0.1.3",
    "lodash": "~2.4.1"
  },
  "devDependencies": {
    "browserify": "~3.18.0",
    "grunt-browserify": "~1.3.0",
    "grunt-browserify2": "~0.1.8",
    "grunt-contrib-watch": "~0.5.3"
  },
  "engine-strict": true,
   "engines": {
    "node": ">=0.11"
  }
}
